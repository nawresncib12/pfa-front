// import styled from "styled-components";
import {
  LoginCard,
  LoginContainer,
  LoginForm,
  LoginTitle,
  NotRegistered,
  NotRegisteredLink
} from "../login/Login.styles";

export const SignupContainer = LoginContainer;

export const SignupCard = LoginCard;

export const SignupTitle = LoginTitle;

export const SignupForm = LoginForm;

export const AlreadyRegistered = NotRegistered;

export const AlreadyRegisteredLink = NotRegisteredLink;
